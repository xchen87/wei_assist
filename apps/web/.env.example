# Copy to .env.local and fill in to connect the assistant (Settings -> AI).
# Without it the chat dock says so plainly and the rest of the app runs
# unchanged. Server-side only — never expose this to the browser.
ANTHROPIC_API_KEY=""
