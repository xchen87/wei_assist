import { EmptySection } from "@/components/plan/empty-section";

export default function TaxPage() {
  return (
    <EmptySection
      title="Tax"
      description="Bracket position, realized and unrealized gains, loss harvesting, and Roth conversion room will show here once the household's tax data source is connected."
    />
  );
}
